cd leaks when using . and ..
trim_path should delete ./
2 heredocs exit when doing 2 signals
#ls > a when a is a directorys leaks "a"
#ls > leaks
ls > a > b > c leaks
export a="jomla mofida" then 'ls > $a' leaks
ambiguious redirection happens when the dir is empty or has spaces (e.g cd 'z b i')
ls > *
